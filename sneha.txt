DEFINE CHANNEL(SVRCON) CHLTYPE(SVRCONN)
ALTER QMGR CHLAUTH (DISABLED)
SET CHLAUTH (SVRCON) TYPE (ADDRESSMAP) ADDRESS(10.250.146.51) MCAUSER(mqm)
REFRESH SECURITY TYPE(CONNAUTH)



SET CHLAUTH(SVRCON) TYPE(BLOCKUSER) USERLIST('nobody')
SET CHLAUTH(SVRCON) TYPE(ADDRESSMAP) ADDRESS(*) USERSRC(NOACCESS)
SET CHLAUTH(SVRCON) TYPE(ADDRESSMAP) ADDRESS('*')  USERSRC(CHANNEL)
REFRESH SECURITY TYPE(AUTHSERV)

ALTER AUTHINFO(SYSTEM.DEFAULT.AUTHINFO.IDPWOS) AUTHTYPE(IDPWOS) CHCKCLNT(NONE) CHCKLOCL(NONE)
ALTER CHL(SVRCON) CHLTYPE(SVR) MCAUSER('NOAUTH')

STOP CHL(SVRCON)
START CHL(SVRCON)
SET CHLAUTH(SVRCON) TYPE(ADDRESSMAP) ADDRESS('*') CHCKCLNT(ASQMGR) MCAUSER('mqm')





 /opt/mqm/samp/bin/amqsput FCRMQ_REQ TPH_MASTER

  /opt/mqm/samp/bin/amqsget FCRMQ_REQ TPH_MASTER

 echo "DISPLAY QSTATUS(FCRMQ_REQ)" | TPH_MASTER


echo "DISPLAY CHANNEL(SVRCON)" | TPH_MASTER

